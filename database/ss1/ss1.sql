create database student_management;
create table teacher (
	id varchar(100),
	name varchar(100),
	age tinyint,
	country varchar(100)
	);
create table class (
	id varchar(100),
    name varchar(100)
    );